{
     "builds": [
       {
         "src": "pocketbase",
         "use": "@vercel/go"
       }
     ],
     "routes": [
       {
         "src": "/(.*)",
         "dest": "/pocketbase"
       }
     ],
     "functions": {
    "api/*.go": {
      "runtime": "@vercel/go"
    }
     }
}

  
